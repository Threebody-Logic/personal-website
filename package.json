{
  "name": "personal-blog",
  "version": "1.0.0",
  "description": "个人BLOG",
  "main": "index.js",
  "author": "Threebody-Logic",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "@vuepress/theme-blog": "^2.3.3",
    "vuepress": "^1.9.9"
  },
  "scripts": {
    "docs:dev": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress dev docs",
    "docs:build": "SET NODE_OPTIONS=--openssl-legacy-provider && vuepress build docs"
  }
}
